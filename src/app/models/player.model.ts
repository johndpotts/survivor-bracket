import { Survivor } from './survivor.model';

export class Player {
    playerId: number;
    firstName: string;
    lastName: string;
    points: number;
    survivors: number[];
    imagePath: string;
}